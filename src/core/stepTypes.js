//so that both the renderer.js and animationEngine.js get the steps from neutral files instead of each other 
export const STEP_TYPES = {
    COMPARE: "compare",
    SWAP: "swap",
    SORTED: "sorted",
    OVERWRITE: "overwrite"
}; 